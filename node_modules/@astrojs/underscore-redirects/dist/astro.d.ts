import type { AstroConfig, RouteData } from 'astro';
import { Redirects } from './redirects.js';
interface CreateRedirectsFromAstroRoutesParams {
    config: Pick<AstroConfig, 'output' | 'build'>;
    routes: RouteData[];
    dir: URL;
    dynamicTarget?: string;
}
/**
 * Takes a set of routes and creates a Redirects object from them.
 */
export declare function createRedirectsFromAstroRoutes({ config, routes, dir, dynamicTarget, }: CreateRedirectsFromAstroRoutesParams): Redirects;
export {};
